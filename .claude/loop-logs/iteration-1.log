Confirmed: my commit touches only the 7 feature files. `sw-cleanup-test.sh` (3 failures: stale-heartbeat dry-run, summary counts, --force hint) is **untouched by my change** and is documented in this session's memory context as a known pre-existing failure. The `npm test` `&&`-chain merely halted there before reaching my suite, which I verified independently passes (56/0).

## Summary

The **Intelligent Retry Strategy Engine** is fully implemented, tested, and committed (`e1735e6c`).

**Delivered (`scripts/lib/retry-strategy.sh`, 373 lines):**
- **4-category taxonomy** (`retry_category_of` / `retry_classify`) — maps any failure to `recoverable-transient` / `recoverable-escalation` / `context-exhausted` / `unrecoverable`. Delegates to `recovery_classify_error` when loaded, self-contained grep fallback otherwise.
- **Memory integration** (`retry_memory_lookup`) — guarded wrapper over `memory_query_fix_for_error`; surfaces proven fixes and raises confidence; degrades to empty when memory/jq absent.
- **Confidence scoring** — bash-safe integer math, clamped to `[0.05, 0.99]`.
- **Escalation ladder** (`retry_escalation_target`) — `same → sonnet → opus → session-restart → human`, bounded so it always terminates.
- **Decision orchestrator** (`retry_decide`) — emits decision JSON + `retry.decision` event; min-confidence and max-attempts gates guarantee termination.
- **Metrics** (`retry_record_outcome` / `retry_metrics`) — per-strategy success rate to a metrics JSONL + `retry.outcome` events; closes the loop into failure memory.
- **CLI dispatch** for tests/debugging.

**Integration (guarded, non-fatal if lib absent):** `sw-loop.sh` (post-test-gate outcome recording + decision), `daemon-failure.sh` (informs the retryable escalation branch), config defaults in `sw-daemon.sh`.

**Verification:** new suite 56/56; all directly-affected suites green — daemon 66, daemon-failure 34, loop 68, auto-recovery, docs 18. Version consistent (3.3.0), bash 3.2 compatible, no TODOs.

All spec goals are met and the acceptance criterion holds (no regressions introduced; the only failing tests are pre-existing and unrelated to this feature).

LOOP_COMPLETE
