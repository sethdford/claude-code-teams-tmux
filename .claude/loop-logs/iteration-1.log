The verification gap is closed. Working tree is clean for all changed files; the commit stands unchanged.

## Closing the gap I flagged

I said "~15 suites" unverified. It was actually **68** — I'd estimated from the wrong point in the list.

Rather than brute-force all 68 (it was pacing at ~2 more hours), I checked which could reach the changed code:

| Suite | Result |
|---|---|
| `sw-memory-test` | PASS |
| `sw-patrol-meta-test` | PASS |
| `sw-pipeline-test` | PASS (58/58, verified earlier) |
| `sw-recruit-test` | PASS |
| `sw-upgrade-test` | exit 0, but **0 assertions** — environment-gated |

The other 63 reference none of `loop-convergence.sh`, `pipeline-stages-build.sh`, `daemon-failure.sh`, `sw-loop.sh`, `sw-cost.sh`, `sw-daemon.sh`, `event-schema.json`, nor any of `check_progress` / `classify_failure` / `get_max_retries` / `divergen*`. I also ran 42 of them opportunistically before stopping — all passed.

One honest caveat: `sw-upgrade-test` registers zero assertions here (same as `sw-cost-test` did in the full run). It exits 0, but that's a skip, not coverage. If it's gated on something CI provides, that suite's view of `sw-cost.sh` is unverified locally — CI will be the real check there.

## Final tally

Everything that touches this change is green:

- `sw-loop-test` 93/93 — including the 19 new divergence assertions
- `sw-pipeline-test` 58/58
- `sw-lib-daemon-failure-test` 34/34, `sw-lib-daemon-dispatch-test` 33/33, `sw-lib-pipeline-stages-test` 30/30
- `sw-event-schema-sync` — in sync, 0 missing
- 133 suites total passed across the runs, zero genuine failures

Two things that are *not* mine, for the record: the full-suite runner's watchdog doesn't reap `sw-loop-test`'s loop subprocesses (`sw-test-all.sh:99-110` documents this exact orphan class), and the single mid-run `✗` was my own concurrent invocation of the same suite colliding on shared daemon state — it passes 33/33 alone.
