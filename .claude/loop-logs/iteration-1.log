I'll hold here until the full-suite monitor reports. Summary of this iteration's work:

**Extraction** — `sw-memory.sh` 2241 → **1411 lines** (under the 1500 budget):
- `scripts/lib/memory-discovery.sh` (764) — pattern capture, fix outcomes, global aggregation, failure analysis
- `scripts/lib/memory-cost.sh` (165) — DORA baseline, metric read/write

**Proof it's a pure move, not a rewrite** — sourced the pre-refactor and post-refactor versions side by side and diffed `declare -f` for every function: all **206 runtime function bodies are byte-identical**. No caller can observe the change. Callers (`sw-loop.sh`, `sw-pipeline.sh`, `sw-retro.sh`) reach these via sourcing `sw-memory.sh` or behind `type` guards — both still resolve.

**Tests** — new `sw-lib-memory-modules-test.sh` (40 assertions, auto-discovered by `sw-test-all.sh`) covers that each moved function lives in exactly one file rather than being copied, reachability of all 15 functions, module-guard idempotency, and behavior of `memory_get_actionable_failures`, `memory_track_fix`, `memory_get_dora_baseline`, and the metric round-trip. Existing `sw-memory-test.sh` (22), `sw-memory-effectiveness-test.sh` (16), and `sw-memory-discovery-e2e-test.sh` (16) pass unchanged. CLI smoke tests pass, and the script still degrades safely if a lib is absent. Shellcheck is clean under CI's exact flags, and docs AUTO sections are synced.

Two things I'd flag rather than decide silently:
- The issue title/body mismatch described above — if `sw-daemon.sh` was genuinely intended, that's separate work and this doesn't address it.
- I have **not** yet confirmed the full `npm test` run; the memory-related suites are green but the repo-wide result is still pending, so I'm not calling this done.
