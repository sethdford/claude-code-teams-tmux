The feature is fully implemented, tested, and reviewed. All code is committed. Here's the summary of what was delivered for **Pipeline Progress Estimation with Real-Time ETA and Completion Percentage** (issue #692):

## Completed (all 13 tasks)

**Core estimation library** (`scripts/lib/pipeline-eta.sh`) — single source of truth:
- `eta_percentile` / `eta_filter_outliers` (IQR method) statistics
- `pipeline_eta_estimate` → `{progress_pct, eta_seconds, eta_p90_seconds, confidence, basis, ...}`
- `pipeline_eta_format` → `Progress: 42% (~18 min remaining)`
- 24h cache of the duration map in the intelligence cache (`.eta` key, non-clobbering atomic write)
- Graceful `stage_count` fallback when history is insufficient (`Progress: 4/9 stages`)

**Data layer** (`scripts/sw-db.sh`): `query_stage_durations()` with input validation (injection-guarded regex) returning per-stage duration arrays.

**State** (`scripts/lib/pipeline-state.sh`): `stage_timings:` persisted in `write_state` and restored on resume.

**CLI** (`scripts/sw-status.sh`): shows Progress %/ETA under the stage bar.

**Dashboard**: TS mirror in `server.ts` (`computeEtaEstimate`, numerically identical to bash incl. `awk '%d'`/`Math.trunc` truncation), `EtaEstimate` type in `api.ts`, progress bar + ETA badge in `overview.ts` + `styles.css`.

**Tests**: `sw-pipeline-eta-test.sh` (34 tests: percentiles, IQR, fallback, P50/P90, cache TTL, injection guard, bash/TS parity fixture), registered in `package.json`.

## Verification
- New suite: 34/34 pass. Affected suites (sw-db, sw-status, sw-lib-pipeline-state, sw-adaptive, sw-pipeline-vitals): all green.
- Code review: production-ready, no bugs ≥80% confidence, parity confirmed, bash 3.2 compatible, SQL-injection safe.
- The one non-green suite (`sw-cleanup-test.sh`, 3/24) is pre-existing and environmental — files untouched by my commits, recorded to memory.

LOOP_COMPLETE
