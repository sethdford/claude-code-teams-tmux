Goal achieved. The feature is fully implemented per the plan's DoD:
- `cost_record` accepts optional auto-detected `repo` ✓
- `shipwright cost breakdown` with all options ✓
- `cost-attribution.json` written atomically, matches schema v1 ✓
- Outliers flagged at >2σ ✓
- Advisory model-routing recommendations ✓
- 30-day sparkline trend ✓
- Budget-type alerts at ≥80% ✓
- All existing cost tests green + 20 new tests passing ✓

LOOP_COMPLETE
