Integration complete. Summary of what was done this iteration:

1. **Verified** `scripts/lib/project-detect.sh` (649 lines) and `scripts/sw-project-detect-test.sh` (434 lines, 43 tests passing) were already implemented
2. **Registered** the test suite in `package.json` `npm test`
3. **Added** `prep_project_detect()` to `scripts/sw-prep.sh` that:
   - Sources project-detect.sh and runs `project_detect_all`
   - Displays detected type/framework/test_cmd/build_cmd/template+confidence
   - In `--interactive` mode, asks for confirmation
   - Atomically merges results into `.claude/daemon-config.json` (preserves existing keys via `jq` merge)
4. **Wired** the call into the main prep flow before `prep_smart_detect`
5. **Verified**: bash syntax ok, all 43 project-detect tests pass, all 13 prep tests pass

All checklist items from the task list are complete. The goal is fully achieved.

LOOP_COMPLETE
