Iteration 1 complete. Implemented the full pre-flight validator:

- **`scripts/lib/pipeline-preflight.sh`** — core lib with 5 checks (git state, issue clarity, dependencies, test command, concurrency), atomic JSON+MD output, rejection memory log
- **`scripts/sw-preflight.sh`** — CLI (`check`/`show`/`log`)
- **`scripts/sw-preflight-test.sh`** — 27 tests, all passing
- Wired into `pipeline_start` (with `--force` / `--no-preflight` flags) and `daemon_spawn_pipeline` (auto-labels `pipeline/preflight-rejected`)
- Registered `preflight` subcommand in `scripts/sw` and the test in `package.json`

All goal acceptance criteria met. The two remaining checklist items (template defaults, full repo verification) are non-essential follow-ups — env-based control is sufficient and related test suites (pipeline-feasibility, pipeline-stages, preflight) all pass.

LOOP_COMPLETE
