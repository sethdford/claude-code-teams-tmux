The feature works end-to-end:
- **$3 budget** → ceiling $2.70 → selects `cost-aware` ($2.27, the most capable that fits)
- **$0.01 budget** → nothing fits → falls back to cheapest `fast` + warning

All acceptance criteria are met and verified:

| Criterion | Status |
|---|---|
| `cost preview <template> [complexity]` with per-stage breakdown | ✓ |
| `cost preview --all` sorted by cost | ✓ |
| `cost select` budget-aware (unlimited→default, fits→most capable, none→cheapest+warn) | ✓ |
| `--json` valid for all three (`jq -e .` passes) | ✓ |
| Respects each template's actual `enabled` stages + per-stage model routing | ✓ |
| Emits `cost.template_selected` event | ✓ |
| New `sw-cost-preview-test.sh` (33 tests) passing, registered in `package.json` | ✓ |
| `sw-cost-test.sh` & `sw-model-router-test.sh` pass (estimate output byte-compatible; the one `chain execute` failure is pre-existing and unrelated) | ✓ |
| Bash 3.2 compatible, `set -euo pipefail`, shellcheck clean, `VERSION` synced, atomic via `jq -n`/`jq --arg` | ✓ |
| `.claude/CLAUDE.md` documents the new subcommands | ✓ |

**What was built:**
- `scripts/lib/cost-preview.sh` — single source of truth for per-stage tokens (`_cp_stage_tokens`), model resolution (`_cp_stage_model` with layered fallback), and the four public functions (`cp_estimate_template`, `cp_preview_one`, `cp_preview_all`, `cp_select`).
- Wired `preview`/`select` subcommands + help into `sw-cost.sh`.
- Refactored `sw-model-router.sh`'s `estimate_cost` to reuse `_cp_stage_tokens` (with inline fallback), keeping output identical.

Work is committed (`66db7ab3`). The goal is fully achieved.

LOOP_COMPLETE
