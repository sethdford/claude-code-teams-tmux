Committed `0bbc4d81`.

**What I did:** added an HTML comment at the top of `README.md` telling maintainers that version numbers/badges are generated and to use `shipwright version bump` / `version check` instead of editing by hand.

**Tests run** (targeted — the change is a comment in one markdown file, so I ran the version check plus every suite that touches `README.md` rather than the full 141-suite run):

| Suite | Result |
|---|---|
| `check-version-consistency.sh` | consistent at 3.3.0 |
| `sw-docs-test.sh` | 18/18 |
| `sw-docs-agent-test.sh` | 14/14 |
| `sw-doc-fleet-test.sh` | 48/48 |
| `sw-adversarial-review-test.sh` | 29/29 |
| `sw-dod-scorecard-test.sh` | 32/32 |

**Skill-required sections:**
- *Test pyramid / coverage targets / critical paths* — skipped as not applicable: this change adds no code and no new behavior, so there is nothing to write unit, integration, or E2E tests for. The relevant verification was regression-only (existing suites still pass).
- *What to document*: `README.md` — one maintainer comment block above the logo. *What to skip*: the release process itself is already documented in `.claude/CLAUDE.md` ("Maintainer / Release"), so the comment points at the commands rather than duplicating them. *Audience*: maintainers editing the README, who need one line of redirection, not a procedure.

**Pre-existing issue found, not fixed** (out of scope): `scripts/sw-adversarial-review-test.sh:1` has a mangled shebang — `#\!/usr/bin/env bash` — so running it directly falls through to `sh` and dies on `set -o pipefail`. It's the only script in `scripts/` with this, and `npm test` invokes every suite with an explicit `bash`, so CI is unaffected. Worth a one-character fix in a separate change.

Goal achieved.

LOOP_COMPLETE
