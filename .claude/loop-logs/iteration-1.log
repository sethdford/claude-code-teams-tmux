The implementation is complete and fully verified. Let me summarize what was delivered against the goal.

## Summary

Implemented the **Intelligence Feature Impact Analyzer with A/B Test Framework** (issue #661), reachable via `shipwright intelligence impact`.

**New files:**
- `scripts/sw-intelligence-impact.sh` (652 lines) — the analyzer with subcommands:
  - `record` — validated, atomic append of per-run experiment records
  - `analyze` — cohort + per-feature success rate / cost / duration / failure-type breakdown (`--json`)
  - `report` — monthly ROI report with KEEP/DISABLE/INCONCLUSIVE recommendations
  - `apply` — gated (n≥20/cohort) auto-disable of negative-ROI features in `daemon-config.json` with backup + atomic write + parse-validation + explanatory note; `--dry-run` is read-only
  - `run-pair` — active harness running an issue twice (intel on/off) under a shared `experiment_id`
  - `status` / `help`
- `scripts/sw-intelligence-impact-test.sh` — **39 tests, all passing**
- `.claude/docs/intelligence-validation.md` — full methodology (cohorts, impact formula, ROI, significance gate, auto-disable safety, re-enabling)

**Modified:**
- `scripts/sw-intelligence.sh` — `impact)` dispatch + help entry
- `scripts/lib/pipeline-execution.sh` — best-effort `record` hook on **both** completion and failure (never affects pipeline exit status, so cohorts see real success/failure variation)
- `package.json` — test registered (fixed a separator I briefly broke)
- `.claude/CLAUDE.md` — command table, Intelligence Layer subsection, AUTO tables synced

**Verification:** impact (39), intelligence (12), docs (18), pipeline-stages (30), and the full pipeline E2E (58) suites all pass; version consistent at 3.3.0; shellcheck clean; bash 3.2 compatible with atomic writes and `jq --arg`/`--argjson` throughout. All Definition-of-Done items met. Committed as `4a6e08d1`.

I also diagnosed that `shipwright docs sync` was aborting on a stray gitignored local artifact (`Pipeline-Design-190.md` with a placeholder `AUTO:section-id` marker) — unrelated to this change and not present in CI.

LOOP_COMPLETE
