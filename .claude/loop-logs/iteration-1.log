Iteration 1 complete. I implemented and committed the foundational chunk of the Smart Template Recommendation Engine:

**Delivered this iteration:**
- `scripts/lib/template-recommender.sh` — deterministic weighted-scoring core. Considers labels, description keywords, a complexity heuristic, repo size, test coverage, and historical per-template success (via the bandit selector when loaded). Returns `{template, confidence, reasoning[], scores{}, signals{}}` with separation-based confidence. Offline-safe (`NO_GITHUB`), bash 3.2 compatible.
- `scripts/sw-template-recommender.sh` — CLI with `recommend`, `explain`, `feedback`, `accuracy` (supports `--issue`/`--goal`/`--labels`/`--json`), plus an append-only JSONL feedback loop and a recommended-vs-actual accuracy report.
- `scripts/sw-template-recommender-test.sh` — 36 assertions (scoring, output contract, edge cases, repo context, feedback loop, CLI integration, error handling) — **all passing**.
- Wired the `template` route into `scripts/sw` + help; registered the test in `package.json`.
- **shellcheck clean.**

**Verified behavior:** docs/typo→`fast`, bug→`standard`, hotfix→`hotfix`, security/auth→`enterprise`, refactor/architecture→non-trivial; feedback round-trips to correct accuracy %.

**Remaining for next iterations** (goal not yet fully achieved):
- Task 7: refactor `daemon-triage.sh:select_pipeline_template` to delegate to `recommend_template`
- Task 8: `--auto-template` flag in `pipeline-cli.sh`/`sw-pipeline.sh`
- Task 9: daemon auto-apply when confidence > 80% + `emit_event`
- Task 12: document in `.claude/CLAUDE.md`
- Task 13: full `npm test` regression check

I'm deliberately not declaring `LOOP_COMPLETE` — the daemon/pipeline integration and docs are still pending.
