Committed. Summary of changes:

- **`scripts/lib/session-restart.sh`** — added `restart_generate_brief_json()`, `restart_track_outcome()`, `restart_success_rate()`; wired brief JSON into `restart_before_restart()`.
- **`scripts/lib/loop-iteration.sh`** — `compose_prompt()` now reads `restart-brief.json` and injects "What NOT to Repeat" / "Recommended Next Direction" / "Files to Focus On" into the fresh-session prompt.
- **`scripts/sw-loop.sh`** — calls `restart_track_outcome` on successful completion and on each failure exit path.
- **`scripts/sw-pipeline-vitals.sh`** — dashboard shows restart history count and success rate.
- **`scripts/sw-session-restart-test.sh`** — 5 new tests + fixed pre-existing `set -e` issue with `((PASS++))`. 17/17 tests pass.
