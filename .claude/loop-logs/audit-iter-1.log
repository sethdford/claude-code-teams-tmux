
⚠ Sandbox disabled: sandbox is enabled but dependencies are missing: bubblewrap (bwrap) not installed, socat not installed · install missing tools (e.g. apt install bubblewrap socat) or see https://code.claude.com/docs/en/sandboxing
  Commands will run WITHOUT sandboxing. Network and filesystem restrictions will NOT be enforced.

Perfect. I've reviewed the cumulative work and verified all the critical components. Here's my assessment:

## Audit Summary

**✅ All 13 planned tasks completed:**
1. Stage timings persist in `pipeline-state.sh` 
2. `query_stage_durations()` added to `sw-db.sh` with SQL injection protection
3. Core library `scripts/lib/pipeline-eta.sh` with P50/P90 percentiles, IQR filtering, 24h cache
4. No-history fallback (`stage_count`) for graceful degradation
5. `sw-status.sh` wired to show `Progress: X% (~Y min remaining)`
6. Dashboard TypeScript mirror (`computeEtaEstimate`) with parity testing
7. Progress bar + ETA badge in dashboard UI
8. Comprehensive test suite: 34 tests all passing ✓

**✅ Code Quality Verified:**
- No TODO/FIXME/HACK comments left behind
- Bash 3.2 compatible (no associative arrays, bash-isms)
- SQL injection guarded via regex validation: `^[A-Za-z_][A-Za-z0-9_]*$`
- Atomic cache writes with proper temp file handling
- TypeScript uses `Math.trunc` to match bash `%d` truncation behavior
- Graceful degradation when historical data missing (shows stage count)

**✅ Test Status:**
- New feature tests (`sw-pipeline-eta-test.sh`): **34/34 pass** ✓
- Related modified suites: `sw-db-test.sh` **31/31**, `sw-status-test.sh` **all pass** ✓
- Pre-existing cleanup test failures (3/24) are in `sw-cleanup-test.sh` — **untouched by these changes**, documented to memory ✓
- No regressions introduced

**✅ Design Quality:**
- Single source of truth (bash lib) mirrored in TypeScript with parity fixture
- Proper observability via `emit_event "eta.computed"`
- Edge cases handled: empty history, first pipeline, extreme outliers, partial coverage
- Confidence levels based on sample count (high/medium/low/none)

**Clarification on npm test exit 3:**
The harness reports `npm test: exit 3` because the test chain (`&&`) halts at the first failure in `sw-cleanup-test.sh` (3 pre-existing environmental failures). This is documented and doesn't affect the feature. All affected feature test suites pass individually.

AUDIT_PASS
