I'll hold here until the suite reports. Interim status of this iteration's work:

**Fixed this iteration** — `_iter_percentile` in `scripts/lib/adaptive-iterations.sh:170` computed the P90 rank as `int(x + 0.5)` (a *rounded* rank) while documenting itself as nearest-rank. That lands one index low for sample counts 6–9, 16–19, 26–29, … — and 6 is the first count above the 5-sample cohort threshold, so every cohort hit it the moment it qualified. The effect was to discard the slowest historical runs, which is precisely the tail the budget exists to cover. Now a true ceiling, with Test 24 pinning both divergent ranks (6 and 16 samples), the unchanged exact-rank case (10 samples), and the resulting cohort budget of 21.

**Verified, not re-derived** (the feature itself landed in prior iterations):

| Gate | Evidence |
|---|---|
| G1 cohort round-trip | `sw-loop.sh:702` computes `ADAPTIVE_COHORT` unconditionally; `:2583` emits it on `loop.iteration_complete` |
| G2 no jq interpolation | both readers pass values via `--arg` |
| G3 bounded single-pass scan | one `jq -R` each, `ITERATIONS_SCAN_LINES` / `ITERATIONS_LOOKBACK` |
| G4 CI registration | present in `package.json` `test:legacy-chain` |

**Suite results so far:** `sw-adaptive-iterations-test.sh` 56/0 (was 52, target was ≥38), `sw-loop-test.sh` 74/74, `sw-daemon-test.sh` 66/0, `shellcheck` clean on both changed files, `sw-event-schema-sync.sh` reports zero drift, `sw-docs.sh check` clean.

On the reported daemon failure from iteration 1 (`classify_failure wired into retry logic`): not reproducible — `scripts/lib/daemon-failure.sh:196` calls `classify_failure` 36 lines into `daemon_on_failure()`, within the test's `grep -A 50` window, and the suite passes 66/0. It was a transient mid-commit state, not a defect.

I have not declared completion — the full `npm test` chain is still running, and I won't call the goal achieved on a partial result.
